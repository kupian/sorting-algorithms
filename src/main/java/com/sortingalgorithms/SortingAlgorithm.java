package com.sortingalgorithms;

/**
 * The SortingAlgorithm interface defines the contract for sorting integer arrays.
 * Implementations of this interface should provide the logic for sorting an array
 * of integers and return the sorted array.
 */
public interface SortingAlgorithm {
    int[] sorted(int[] arr);
}
